package basic;

public class AccessModifier {
	// AccessModifier / Access Specifier
		// Access
		
		// public : No Restrictions : Have to access from any packages
		// private : Fully Restricted; Have to access from same class only
		// protected : Have to access in same package
		// Default: Same behavior like protected
		
		// if I declare a variable is public then I can call it from any class or packages
		// if I declare any variable is static , it means I can call that variable by its class name.
		public static void main(String[] args) {
			
		}

}
